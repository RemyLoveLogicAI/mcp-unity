interface:
  display_name: "Mcp Unity"
  short_description: "Repo-specific patterns and workflows for mcp-unity"
  default_prompt: "Use the mcp-unity repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true